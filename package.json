{
  "name": "new-yno-soundtrack",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@violentmonkey/types": "^0.3.2",
    "esbuild": "^0.27.4",
    "fuse.js": "^7.1.0"
  },
  "scripts": {
    "build": "bunx esbuild src/app.ts --bundle --outfile=dist/hook.js --platform=browser; cat header.js dist/hook.js > dist/hook.user.js"
  }
}
